import { ResortsFilterPipe } from './resorts-filter.pipe';

describe('ResortsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResortsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
